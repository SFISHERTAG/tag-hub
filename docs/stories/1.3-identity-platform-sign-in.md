# Story 1.3 — OTP sign-in via Firestore

**Epic:** 1 — Foundation and access
**Status:** Done

## Story

As a TAG staff member or client user, I want to sign in to the Hub with a 6-digit OTP sent to my email, so that the app knows who I am before it shows me anything.

## Context

Nothing in the Hub is currently authenticated. Every route reads
`GHL_LOCATION_ID` from the environment and serves the same single tenant to
anyone who opens it. That is fine for development against TAG's own
sub-account and unacceptable the moment a client user or a second tenant
exists.

OTP via email is simpler than password management and works for both TAG staff and client users.

## Acceptance criteria

1. An unauthenticated request to any route other than the sign-in page
   redirects to sign-in.
2. Email entry generates a 6-digit OTP (valid for 10 minutes), hashed and
   stored in Firestore (`authCodes/` collection).
3. User enters email + OTP; server verifies against Firestore and creates a
   Firebase Admin Auth session (custom token, httpOnly session cookie).
4. The session is verified server-side on every request. A forged or expired
   token is rejected, not trusted from the client.
5. Sign-out clears the session and returns to sign-in.
6. `GHL_LOCATION_ID` no longer determines what a signed-in user sees. It may
   remain as a development fallback only.

## Tasks

- [x] `authCodes/` collection in Firestore (email, hashed code, created_at, expires_at) — `lib/auth/otp.ts`
- [x] Email send integration (sendgrid or similar) — `lib/auth/mailer.ts`
- [x] POST `/api/auth/request-otp` — generate and send OTP (built as `/api/auth/otp/request`)
- [x] POST `/api/auth/verify-otp` — verify code and create session (built as `/api/auth/otp/verify` + `/api/auth/session`)
- [x] Session cookie, httpOnly and secure in production (`app/api/auth/session/route.ts`)
- [x] Sign-in page with email + OTP entry (`app/signin/`)
- [x] `middleware.ts` gating all routes except sign-in and `/api/oauth/*` — built as `proxy.ts` (Next 16 renamed middleware to proxy)
- [x] Sign-out (`app/api/auth/signout/route.ts`, revokes refresh tokens)
- [x] Seed initial users (`scripts/create-user.mjs`); location/role claims populated per 1.4

_Verified against code 2026-08-18. AC corrected to match the actual, deliberate design: OTP codes hashed in Firestore, sessions via Firebase Admin Auth custom tokens. This was decided same-day as the original Firebase password sign-in was replaced (commit ac31ec5), not a drift from a Postgres spec — a prior memory note claiming "Postgres, not Firebase" was itself wrong and has been corrected._

## Dev notes

`/api/oauth/*` must stay unauthenticated — it is the GHL install callback and
GHL will not carry a Hub session. It has its own CSRF protection via the
`state` parameter and cookie comparison.

Verify the session server-side on every request. The whole tenant-isolation model rests on the role and location list
being trustworthy.

OTP cleanup: expired codes can be deleted via a scheduled job or vacuum on request.
