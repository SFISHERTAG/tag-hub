# Story 10.6 — Widget dashboard and the clients book

**Epic:** 10 — Angular migration
**Status:** In Progress

## Story

As a CSM, CS Director or executive, I want the configurable dashboard and the
clients book in Angular, with widgets resolved at runtime by id and permission
so the shell never learns an integration's name.

## Story, the second half

As a client owner, I want my own dashboard to show my tenant's numbers, which
means the widget data path resolves my location from my session.

## Context

Two surfaces, one story, because they share the widget registry and most of
their data. `/dashboard` is the configurable grid; `/csm-dashboard` is the book
of business at 1,562 lines of working views (grid, list, kanban, escalation,
plus the client detail modal's four tabs).

`features/clients/` is deliberately **not** an integration module. It shows GHL
pipeline data next to Meta spend next to Drive creatives, and the rule that
integrations may not import each other would make that impossible if it were
one. Cross-integration data arrives through one backend endpoint that does the
joining server-side. That is the constraint that decides this story's endpoint
shape: not one endpoint per integration, but one per view.

The dashboard shell knows no integration by name. Widgets register through
`WidgetRegistry` and are resolved by id plus required permission, loaded through
dynamic `import()` named only in `widget-loaders.ts`, the one declared exemption
to the boundary rule.

## Acceptance criteria

1. `features/dashboard/` holds the widget shell and `customize/`, and imports no
   feature module by path.
2. `features/clients/` holds `book/` (grid, list, kanban, escalation),
   `client-detail/` (overview, campaigns, creatives, status) and its own
   widgets.
3. Cross-integration reads for the clients book go through a single endpoint per
   view; no Angular service composes two integrations client-side.
4. Widget entitlement is enforced server-side on both the save path and the read
   path. A saved layout naming a widget the role may not use neither saves nor
   fetches.
5. The layout save round-trips through an endpoint, and the saved layout is keyed
   per `(uid, hat)`.
6. `widget-loaders.ts` is the only file naming a feature by path for widget
   loading.
7. The Next pages these replace are deleted. The sweep is owned by Story 10.8.

   > **Amended 2026-08-27.** Previously: "The Next pages these replace are
   > deleted in the same commit." Unmeetable by construction: the commit it
   > referred to, `198c7684e9ffc4a073f2d543c4487e0885acb90b`, landed without the
   > deletions. Intent preserved above, that a migration must not leave two
   > implementations of one screen. Same amendment as Stories 10.5 and 10.4.
   > Still unmet: 33 `page.tsx` files remain under `legacy/`.
8. `ng build --configuration production && ng lint && ng test --watch=false`
   pass.

## Acceptance criteria verdicts

Adjudicated 2026-08-27 against the tree at `8787c50`. Status is `In Progress`
because AC7 is unmet; every other criterion is met.

| AC | Verdict | Evidence at `8787c50` |
|---|---|---|
| 1 | **Met** | `features/dashboard/` holds `dashboard-shell/`, `widget-host/` and `customize/`. It imports no feature module by path: a search for any `features/` import inside `features/dashboard/` returns nothing. |
| 2 | **Met** | `features/clients/book/` holds all four views (`client-grid`, `client-list`, `client-kanban`, `client-escalation`), `client-detail/` holds `overview-tab`, `campaigns-tab`, `creatives-tab` and `phase3-tab`, and `clients/widgets/` holds four widgets. One naming note rather than a defect: the criterion and `docs/frontend-file-tree.md` both call the fourth tab "status", and it is implemented as `phase3-tab`. Same tab, different word. |
| 3 | **Met** | `features/clients/` imports no integration feature at all: no import of `features/ghl/`, `features/meta/`, `features/drive/` or `features/slack/` anywhere in the module. Composition happens server-side. |
| 4 | **Met** | Enforced on both paths in `app/api/dashboard/config/route.ts`. The GET filters saved placements before returning them, and the PUT builds `allowed` from `getAvailableWidgets(config.role)` and rejects any placement not in it. A layout naming a widget the role may not use neither saves nor fetches. |
| 5 | **Met** | The layout round-trips through `GET`/`PUT /api/dashboard/config`, and `loadDashboardConfig(session.uid, session.currentRole)` keys it per user and per hat. |
| 6 | **Met** | `web/src/app/widget-loaders.ts` is the only file naming a feature by path for widget loading. `app.config.ts` imports `registerWidgetLoaders` from it and names no feature itself. |
| 7 | **Unmet** (criterion amended, see above) | 33 `page.tsx` files remain under `legacy/`. |
| 8 | **Met** | Same tree and same run as Stories 10.5 and 10.4: `ng build --configuration production`, `ng lint` and `ng test --watch=false` each exit 0, 609 tests in 70 files, no budget warning. Exit codes read directly. |

## Endpoint inventory (Story 11.5)

Measured at `40e9aa6` by `node scripts/inventory-endpoints.mjs --area=<area>`.
Re-run before starting; the surface moves.

| Area | Server Actions | Existing routes | Pages importing `lib/` |
|---|---|---|---|
| `csm-dashboard` | 7 across 7 files | 0 | 1 of 1 |
| `dashboard` | 1 | 0 | 2 of 2 |
| **Total** | **8 across 8 files** | **0** | **3 of 3** |

Fewer actions than 10.5 and a larger surface behind them: the seven
csm-dashboard actions each back a different view or tab, and all of them now
carry the ownership gate added by the resweep, which has to survive the move to
an endpoint rather than being left behind with the action.

The one `dashboard` action is the layout save, which is also where the widget
allowlist is enforced. Its endpoint must enforce the same thing on both the save
path and the read path.

## Tasks

- [x] Inventory the endpoints this story needs, per Story 11.5
- [x] Endpoint for the dashboard layout: load and save, with the widget
      allowlist enforced on both
- [x] `features/dashboard/` shell and grid, widgets resolved by id + permission
- [x] `features/dashboard/customize/` picker
- [x] Endpoints for the clients book: assigned, team and department scopes
- [x] `features/clients/book/` with the four views
- [x] Endpoint per client-detail tab, joining server-side
- [x] `features/clients/client-detail/` with its four tabs
- [ ] The clients widgets: client-health, portfolio, team-health-rollup,
      department-overview, team-performance
- [ ] Delete the replaced Next pages and repoint the routes
- [ ] Carried forward from the resweep, and not to be lost in the port:
  - [x] Health scores, status badges and escalation buckets derived from
        `getMockMetrics` render with a disclosure. They are identical for every
        client and they feed real escalation decisions; `ClientHealth.is_sample`
        marks the data so a consumer can tell without knowing its origin
  - [x] Multi-page dashboards actually switch pages. The Next version linked to
        `?page=<id>` and never read the parameter, so every tab rendered the
        same page
  - [ ] One unreachable tenant does not empty the whole book. The Next
        portfolio used `Promise.all` and lost every client to one bad lookup;
        settle per tenant and say how many are missing
  - [ ] The widget allowlist is a permission, not a picker filter. Enforced on
        save and again before any data is fetched for a widget id

## Dev notes, why the mock-metrics disclosure is an acceptance concern

`getMockMetrics()` ignores its client id and returns the same four numbers for
every client. Those numbers become the health score, the status badge and the
escalation bucket that a CSM acts on. The Next app rendered them with no
disclosure until the resweep, which is how a CSM ends up escalating on an
invented figure.

Porting the widget without porting the disclosure recreates that exactly, and it
will look like new work rather than a regression. Until per-client spend and
lead targets exist in the schema, the honest state is real-shaped numbers plus a
banner nobody can miss.

## Dev notes, the clients book is not legacy

Epic 3's six stories are Ready and `/csm-dashboard` is working code. Its absence
from the Next nav was a bug, fixed in 10.3, not evidence that the surface was
abandoned. This story ports a live feature.
