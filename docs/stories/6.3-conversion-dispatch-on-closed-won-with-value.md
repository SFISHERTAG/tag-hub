# Story 6.3 — Conversion dispatch on closed-won with value

**Epic:** 6 — Acquisition loop
**Status:** Done

## Story

As TAG, I want to send a "purchase" conversion to Meta with the deal value when a prospect closes, so that Meta optimizes for high-value closes, not just closes.

## Context

Meta Conversions API accepts value in the conversion. Sending `value: 18000` (USD) teaches the algorithm "this ad bought someone who closed a $18k deal."

Without value, the algo treats all closes equal. With value, it learns to prioritize ads that close high-ticket deals.

Data: opportunity marked won (Story 2.6) → get contact → get utmAdId + monetaryValue → send to Meta with value.

## Acceptance criteria

1. Trigger: opportunity marked won via Story 2.6 (closeOpportunity).
2. Extract monetaryValue from the opportunity.
3. Call Meta Conversions API with:
   - event_name: "Purchase"
   - value: monetaryValue (USD)
   - currency: "USD"
   - user_data: hashed email/phone, fbc, fbp, utmAdId
   - event_id: opportunityId (idempotency)
4. Log to Firestore (Story 3.5).
5. On error: log and alert (don't block opportunity close).

## Tasks

- [x] Create `dispatchClosedWon(locationId, opportunityId, contact, value)` function — [lib/meta/conversions.ts](../../lib/meta/conversions.ts)
- [x] Meta Conversions API call with value parameter
- [x] Hashing + user_data assembly — shared with Story 6.2's `dispatchShowed` via `buildUserData`/`postConversionEvent` in the same file, rather than reimplemented
- [x] Idempotency: event_id = opportunityId
- [x] Error handling: non-blocking — verified in [test/dispatch-closed-won.test.ts](../../test/dispatch-closed-won.test.ts), same pattern as 6.2's [test/dispatch-showed.test.ts](../../test/dispatch-showed.test.ts)
- [x] Server action: triggered from Story 2.6 close action — [app/l/[locationId]/pipeline/actions.ts](../../app/l/[locationId]/pipeline/actions.ts), scheduled via `after()` so a Meta outage can't block or slow the close

## Dev notes

Depends on Story 4.1 (Meta pixel).

Value is USD integer (no cents). Validate ≥ 0.

If monetaryValue is 0 (they marked won but no value), send value: 0 anyway — that teaches Meta this ad closed someone but low-ticket.
