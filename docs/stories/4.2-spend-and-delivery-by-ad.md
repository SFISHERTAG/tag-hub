# Story 4.2 — Spend and delivery by ad

**Epic:** 4 — Client owner dashboard
**Status:** Blocked (Meta credentials not actually live — see dev notes)

## Story

As a client owner, I want to see how much I'm spending per ad creative, so that I can see which ones are worth the budget.

## Context

Meta Marketing API returns spend per ad via the Ads API. GHL returns booked appointments and closed deals per contact, which are attributed to an ad via `utmAdId` on the contact record.

This story surfaces spend alone (Meta API). Story 4.4 joins it to outcomes (booked, showed, closed) to compute ROAS per ad.

## Acceptance criteria

1. Fetch spend per ad from Meta Marketing API (daily, last 30 days).
2. Display as a table: Ad name, spend (USD), spend trend (↑/↓ vs 7-day avg).
3. Sort by spend descending (biggest spenders first).
4. "As of" timestamp (Story 4.5) shows when this data was last fetched.
5. No data without Meta setup (Story 4.1) — show a setup notice instead.

## Tasks

- [x] Create `getAdSpend(adAccountId, days=30)` function calling Meta Ads API
- [x] Parse response into { adId, adName, spend, spend7d }
- [x] Component: AdSpendTable with sort + trend indicators
- [x] Page: `/l/[locationId]/dashboard` showing spend table + "as of" timestamp
- [x] Error handling: if Meta API fails, show clear message

## Dev notes

Depends on Story 4.1 (Meta System User setup). Without it, this won't authenticate to Meta API.

Trend calculation: compare 7-day avg spend to overall 30-day avg.

**Partial implementation (2026-08-18):** `getAdSpend()` was built in `lib/meta/ads.ts` as
a prerequisite for Story 4.4, which needs real spend data to join against — it queries
`/insights` at `level: "ad"` for both the full window and a trailing 7-day window, and
returns `[]` (not a throw) when Meta isn't configured, matching AC5's intent. Not marking
this story Done: AC2–4 (a dedicated `AdSpendTable` UI, the standalone spend-only widget,
and the "as of" timestamp from Story 4.5) are still unbuilt. Story 4.4 consumes
`getAdSpend()` directly rather than through a UI this story hasn't produced yet.

**UI + error handling completed, but not marked Done (2026-08-18):**

The task brief for this pass claimed Story 4.1 was "confirmed unblocked as of
2026-08-16" and that Meta auth should now actually work. That claim does not
hold up against the repo: `.env.local` and `.env.example` both have
`META_SYSTEM_USER_TOKEN` / `META_BUSINESS_ID` / `META_APP_ID` /
`META_APP_SECRET` empty, `cloudbuild.yaml` wires no Meta secrets into Cloud
Run at all, and `app/api/meta/status/route.ts`'s own comment already said
"`META_SYSTEM_USER_TOKEN` doesn't exist yet." `docs/meta-live-launch-plan.md`
asserts Phase 2 is done and the token is live, but nothing in the deployable
app actually has it — that doc is aspirational/stale relative to the
codebase, not a reliable signal. Flagged this to Sam directly; decision was
to build the UI now so it's ready the moment the token lands, accepting that
AC1–4 can't be verified against a live Meta response in this pass. **Do not
promote this story to Done off documentation claims alone — verify
`isMetaConfigured()` is actually true in the target environment and that a
real ad account renders real numbers, not just that the code compiles.**

What shipped, built directly on the existing 4.4 code rather than a parallel
implementation:

- [`lib/meta/ads.ts`](../../lib/meta/ads.ts) — existing `getAdSpend(adAccountId, days)`,
  changed so a real API failure now throws `MetaApiError` instead of being
  swallowed into `[]`. Previously "no spend" and "Meta rejected the request"
  were indistinguishable; `lib/dashboard/roas.ts` (Story 4.4) already had an
  outer catch that turns a thrown error into `{ok:false, message}`, so this
  fixes AC "clear message, not blank screen" for both stories without a
  behavior change for existing callers.
- [`lib/dashboard/ad-spend.ts`](../../lib/dashboard/ad-spend.ts) —
  `getAdSpendTable(locationId, days=30)`, new. Looks up the tenant, returns
  `{ok:false, reason:"not_set_up"}` if `metaAdAccountId` is missing (AC5),
  calls `getAdSpend`, computes the ↑/↓/→ trend (7-day avg vs the window avg,
  per the dev note above), sorts by spend descending (AC3), and calls
  `recordMetaFetch` so Story 4.5's existing freshness indicator (already
  rendered in `app/l/[locationId]/layout.tsx` on every page in this tree)
  picks up the fetch time — deliberately not building a second "as of"
  mechanism.
- [`app/l/[locationId]/dashboard/ad-spend-table.tsx`](../../app/l/[locationId]/dashboard/ad-spend-table.tsx) —
  `AdSpendTable`, new.
- [`app/l/[locationId]/dashboard/page.tsx`](../../app/l/[locationId]/dashboard/page.tsx) —
  new page under the existing `/l/[locationId]` tenant-scoped tree (same
  layout/guard as `today`, `pipeline`, `contacts`), not a top-level
  `/dashboard` — that path is already the separate cross-role widget
  dashboard (`app/dashboard/`, Story 4.5's own dev notes flagged this same
  ambiguity). Renders the setup notice, the Meta-error notice, or the table.

Verified: `tsc --noEmit` clean (pre-existing failures elsewhere untouched),
route compiles and redirects cleanly to sign-in under the existing auth
guard with no server errors. Not verified: a live Meta response, since no
environment in this repo currently has real credentials.

## Post-implementation change: dashboard tenancy + widget allowlist (resweep criticals 2, 3)

Files this story references were changed by a cross-cutting security fix, not
by further work on the story itself. Status is unchanged.

Two changes touch this story's surface:

- The dashboard widget fetchers (`day-view`, `pipeline-board`, `roas`,
  `owner-calendar`, `funnel`) now take a `locationId` argument instead of
  resolving one from the global `GHL_LOCATION_ID` env var via
  `devLocationId()`. `app/dashboard/page.tsx` resolves it from the session
  through `resolveDashboardLocation` and access-checks it before fetching.
  Single-tenant behaviour is unchanged; multi-tenant behaviour is now correct.
- `availableFor` in the widget registry is enforced as a permission
  (`canUseWidget` / `filterWidgetsForRole`), rejected on layout save and
  filtered on read, rather than only filtering the customize picker.
