# Story 10.3 — Responsive shell and navigation

**Epic:** 10 — Angular migration
**Status:** Review — completed 2026-08-23. One task rejected as contradicting the theme contract, see Completion Notes.

## Story

As any signed-in user, I want one shell that adapts to my screen rather than two
layouts that drift apart, and a navigation that cannot show me a link my route
guard will refuse.

## Context

CLAUDE.md requires "one responsive shell, not two separate layouts":
`mat-toolbar` + `mat-sidenav` at >=840px, `mat-toolbar` + bottom nav below it,
same component tree, breakpoint-driven.

Phase 2 built that shell, but built it as `app.ts`, the root component. That had
a cost nobody had measured until the initial bundle reached 486 kB against a
500 kB warn budget.

## Acceptance criteria

1. The root component imports nothing but `RouterOutlet`.
2. The shell loads lazily, behind `authGuard`, so a signed-out visitor never
   downloads it.
3. Sidenav and bottom nav are mutually exclusive at every width.
4. Nav items derive from `PermissionService`, and no role sees a link its route
   guard denies.
5. `tag_csd` has a reachable nav set.

## Tasks

- [x] Move the shell from `app.ts` to `layout/shell/`, lazy-loaded behind
      `authGuard`
- [x] Reduce `app.ts` to a bootstrap host with a single `RouterOutlet`
- [x] Restructure `app.routes.ts` so `/signin` sits outside the shell and
      everything else inside it
- [x] `shell.spec.ts` covering breakpoint behaviour and mutual exclusivity
- [x] Nav items derived from `PermissionService` via a single `nav-items.ts`
- [x] `tag_csd` nav set (currently matches zero entries in the Next `nav.tsx`,
      so they get an empty tab bar and their `team_health_rollup` widget is
      unreachable)
- [x] Reconcile every nav-vs-guard mismatch, with the guard as source of truth — was already done; three tests in `app.routes.spec.ts` assert refuses/permits/empty-nav
- [x] Hat switcher, hidden when `availableRoles.length < 2` — shipped by story 10.9 in the user menu, and marked temporary there: 15.D deletes it
- [x] Impersonation banner and the shell offset it drives — `layout/impersonation-banner/`
- [x] ~~Three-state theme toggle~~ — **rejected**, contradicts the theme contract. See Completion Notes
- [x] Fix app identity — title `TAG Cockpit`, manifest `TAG Cockpit` / `TAG`
- [ ] Carried forward from the resweep, and not to be lost in the port:
  - [x] Assertion that no role resolves to an empty nav — exists as "gives
        every hat a nav set rather than an empty bar" over `ROLE_LIST`, in
        `app.routes.spec.ts` rather than a separate `nav-items.spec.ts`. Same
        guarantee, next to the guard tests it depends on
  - [x] ~~Theme toggle `localStorage` guard~~ — moot, no toggle. The hazard it
        names is still real for anything else that reaches for `localStorage`
        from the persistent shell, and nothing currently does

## Dev notes, the bundle split

Measured before cutting. `main.js` was 90 kB, and about 75 kB of that was shell
chrome: `list` 27.6 kB, `sidenav` 27.0 kB, `icon` 11.9 kB, `scrolling` 5.0 kB,
`toolbar` 3.5 kB. All of it downloaded by every visitor, including a signed-out
one whose only destination is `/signin`, a page that uses none of it.

Two further items were in the shared initial chunk for the same reason:
`@angular/forms` at 37.8 kB, used only by the sign-in form, and `button` at
28.5 kB, shared only because the root component also used it. Once the root
stopped importing Material, both followed their real consumers into lazy chunks.

Result: **initial 486.14 kB to 301.38 kB, transfer 116.32 kB to 81.27 kB.** The
shell is now an 81 kB lazy chunk that loads when there is a session to show it
to.

This is why `app.ts` must stay empty. Anything imported there is paid for on
first paint by everyone, forever, and the cost is invisible until someone runs
the numbers. `app.spec.ts` now asserts the root renders a router outlet and no
chrome, so the regression fails a test rather than quietly costing bytes.

The route structure carries the same reasoning: `/signin` sits outside the shell
because rendering it inside would mean downloading a toolbar and sidenav to show
a page that has neither. Feature modules become children of the shell route,
each with its own `permissionGuard`; the shell route itself requires only a
session, because every signed-in role sees the shell.

## Dev notes, navigation

`layout/nav/nav-items.ts` is the single source. One list, both presentations.
The side rail and the bottom bar render from it, which is what makes "one shell,
not two layouts" true rather than aspirational: there is no second list to
drift. A spec asserts the two renderings produce the same count.

`tag_csd` now has a nav set. In the Next app they matched zero entries, so a CS
Director signed in to an empty tab bar and the `team_health_rollup` widget built
for them was unreachable. A director sees the whole department, so they appear
anywhere a CSM does.

The permission rule these entries must obey: an item's list has to be a SUBSET
of what its route guard allows. Showing a link the guard refuses sends people
into a redirect; hiding one the guard permits makes a feature undiscoverable.
When feature routes land, `data.permission` and the entry here are set together.

## Dev notes, defects found by adversarial review

A ten-agent review of the Hot Path library proposal found three real defects in
this repo, all in code written the previous day and all confirmed by hand before
acting:

**`fail()` had diverged across the network boundary.** The server's is
`fail(context, cause: unknown)`; the client's was `fail(context, message,
status?)`. Same name, incompatible arity. The header comment claimed "same shape
on both sides", true of the types and false of the functions. Nobody noticed
because the client-side `fail` had zero callers. Removed it and corrected the
comment to say what is actually true: the types mirror, the constructors do not,
because `errorInterceptor` has already converted an `HttpErrorResponse` into an
`ApiError` before any caller sees one.

**The `*hasPermission` directive had never rendered.** Its only importer was its
own spec, and no template referenced it. It is now what gates every nav item, so
it has a real consumer in the shipped app rather than a test harness built for
it. Its usage example also showed bare role strings, which the pre-commit check
strips before matching and would therefore have been copied into real code.

**`permissionGuard`'s allow path still has not executed.** Zero routes declare
`data.permission`; the only textual match is a doc comment. Every real
invocation takes the PUBLIC_ROUTE early return or the deny branch, so the
permission check itself has never run in the application. Left open
deliberately: it needs a real permission-gated feature route, which arrives with
the first ported feature module, not before.


## Dev Agent Record

### Completion Notes

**Completed 2026-08-23.** Three of the six open tasks turned out to be already
done, one was rejected, and two were built.

**Already done, verified rather than assumed.** The nav-vs-guard reconciliation
has three tests in `app.routes.spec.ts` — a nav item shown to a hat its guard
refuses, a hat the guard permits with no link, and every role having a non-empty
nav over `ROLE_LIST`. That last one is the `nav-items.spec.ts` this story asked
for; it lives in the routes spec instead, next to the guard tests it depends on,
which is the better home. The hat switcher shipped in 10.9 with exactly the
`availableRoles.length < 2` rule specified here.

**Rejected: the three-state theme toggle.** `web/src/styles/_theme.scss` opens
with "TAG Cockpit is dark-only by design (true black has no light-mode
equivalent), so this file sets static values rather than light-dark() pairs."
There is no light theme to toggle to. Building the control would ship three
states of which one works.

This task predates that decision. Left as a struck-through line rather than
deleted, because the next person to read the story will otherwise wonder why a
listed requirement is missing. If a light variant is ever wanted, `_theme.scss`
says exactly how: wrap each override in `light-dark()`. The toggle is the last
step of that work, not the first.

The carried-forward `localStorage` guard goes with it. The hazard it names —
a throw in the persistent shell taking down every authenticated page rather than
one control — is real and still worth remembering for anything else that
reaches for `localStorage` there. Nothing currently does.

**Built: the impersonation banner.** Ported from 3.4. It renders from
`session.impersonation`, which arrives on every session response because the
`hub_impersonation` cookie is httpOnly — a separately-fetched banner would
vanish on reload while the access it describes stayed live.

Three decisions worth recording. It sits *above* the sidenav container rather
than inside it, so it cannot scroll away; someone who has scrolled is exactly
the person who has forgotten whose tenant they are in. It uses
`tertiary-container` rather than `error`, because this is a state to be aware of
and not a fault, and error styling on an ordinary CSM workflow stops being read.
And a failed exit surfaces the error and does **not** navigate: a banner that
disappears while the cookie survives states the opposite of the truth, and every
action after it is still attributed to the impersonated tenant.

**`exitImpersonation` went on `RbacService`,** for the same reason `signOut` did
in 10.9: `layout/` renders the banner and must not import `features/`. It
returns the rebuilt session rather than void, because the hats and locations on
the way out are not the ones on the way in.

**Built: app identity.** Title and manifest both read "web". Now `TAG Cockpit`,
with `TAG` as the short name for a home-screen icon.

### File List

- `web/src/app/layout/impersonation-banner/{*.ts,.html,.scss,.spec.ts}` (new)
- `web/src/app/core/services/rbac.service.ts` (modified — `exitImpersonation`)
- `web/src/app/core/services/{http-rbac,mock-rbac,signed-out-rbac}.service.ts` (modified)
- `web/src/app/layout/shell/{shell.html,shell.ts,shell.scss}` (modified)
- `web/src/index.html`, `web/public/manifest.webmanifest` (modified)
- Four specs (modified — `exitImpersonation` on their stubs)

### Change Log

- 2026-08-23 — Impersonation banner, app identity, theme toggle rejected.
  Status → Review.
