---
baseline_commit: db89f36
---

# Story 14.B — Inline role string audit and periodic scanner

**Epic:** 14 — Off Firestore  
**Status:** Review — implemented 2026-08-23. All 8 production violations fixed, both holes closed, scanner enforcing.  
**Depends on:** Completion of 10.7 (legacy removal)

## Story

As a security maintainer, I need to find all hardcoded role strings in the tree and implement a periodic audit to catch drift, so that the role-isolation constraint is enforced tree-wide rather than only on new commits.

## Problem

The pre-commit hook `scripts/check-story-status.mjs` includes an inline-role check that scans for hardcoded role strings. It is diff-scoped: it only examines files staged for commit.

**Design intent:** Guard against NEW violations entering the tree.  
**Design gap:** Cannot find EXISTING violations. The hook has successfully prevented new inline roles from entering the tree, but three live violations already exist.

CLAUDE.md describes the role constraint as "enforced via pre-commit hook" but is silent about whether that covers the whole tree or only new commits. The gap is documented in `AGENT_COORDINATION.md §3`: a check that reports healthy while scanning a shrinking share of what it claims to cover.

## Audit findings

### Production violations: 8 files, 26 lines, 52 occurrences

**Broken down by scope (files and lines are unit-precise; occurrences = count of role literals in those lines).**

#### Pre-existing violations (3 files, 3 lines, 3 occurrences)

| File | Line | Violation | Severity | Notes |
|---|---|---|---|---|
| `lib/auth/admin.ts` | 175 | `await setUserClaims(uid, [{ role: "tag_exec", locations: [] }]);` | **MOST SEVERE** | Claim-issuing path; goes to story owning promoteToExec |
| `app/api/meta/status/route.ts` | 24 | `if (session.currentRole !== "tag_exec")` | High | Access check; goes to story that wrote Meta status route |
| `scripts/setup-csm-test-data.ts` | 110 | `role: "tag_csm"` | Low | Seed script (scripts/ not production app code); fixable now |

#### Shielded by Hole 2 (5 files, 23 lines, 49 occurrences)

| File | Lines | Occurrences | Notes |
|---|---|---|---|
| `app/api/flow/card/[cardId]/suggestions/route.ts` | 1 | 3 | `const SUGGESTER_ROLES = [...]` |
| `app/api/flow/org/[orgId]/suggestions/route.ts` | 1 | 2 | `const REVIEWER_ROLES = [...]` |
| `app/api/flow/suggestions/[suggestionId]/resolve/route.ts` | 1 | 2 | `const REVIEWER_ROLES = [...]` |
| `lib/dashboard/location-selection.ts` | 9 | 14 | Hardcoded role strings in role lists and access checks |
| `lib/dashboard/widget-definitions.ts` | 11 | 28 | **Role-to-capability map** (see note below) |

**⚠ Widget-definitions.ts note:** The 28 occurrences in 11 lines indicate a structured role-to-capability map, not scattered checks. Conversion to `ROLES.*` should be done wholesale on the entire map, not as 28 separate fixes.

#### Test files (separate scope, 3 files, 10 lines, 10 occurrences)

Role literals in `*.spec.ts` files (app.routes.spec.ts, admin-users.service.spec.ts, ghl.routes.spec.ts) are route-behavior assertions, not production violations. Listed separately for completeness.

### Legacy violations: 135 across 29 files

Legacy violations exist primarily in `legacy/**` and require triage before fixing:
- Some are legitimate role-list definitions (e.g., `legacy/knowledge-base/roles.ts`, `legacy/nav.tsx` hat lists) that may be intended as reference data
- Others are access checks that should be refactored to use `hasAnyRole()` or equivalent
- Story 10.7 will remove the entire `legacy/` directory; violations here do NOT count as regressions once 10.7 lands

**Triage needed:** Before fixing legacy violations individually, clarify whether each is a definition (keep), a check (refactor), or a stale artifact (delete).

## Root cause: Two independent holes in the check

The pre-commit check reports clean while the tree holds:
- **Production:** 26 lines containing 52 role-literal occurrences across 8 files
- **Test files:** 10 lines with 10 occurrences across 3 spec files (separate scope)
- **Legacy:** 135 lines across 29 files (pre-10.7 removal)

Two independent gaps explain this:

### Hole 1: Diff-scoping (documented, by design)

The pre-commit hook `scripts/check-story-status.mjs` only examines staged files:

```javascript
const diff = execSync('git diff --cached --name-only')
  .toString()
  .split('\n')
  .filter(Boolean);
```

**Intent:** Guard against NEW violations entering the tree.  
**Reality:** Cannot find EXISTING violations already in the tree.

This gap is correct-by-design for a commit gate. The issue is that CLAUDE.md describes the role constraint as "enforced via pre-commit hook" without clarifying whether that covers only new commits or the entire tree.

### Hole 2: File-level exemption (not in story, larger impact)

The hook applies a blanket pass to any file that imports or calls auth helpers. From `scripts/check-story-status.mjs:185`:

```javascript
if (!ROLE_HELPER_PATTERN.test(fileDiff) && !ROLE_HELPER_PATTERN.test(fullContent)) {
  // File uses no helpers; check for violations
} else {
  return null; // File is exempt; no violations reported
}
```

**Consequence:** A line like `const SUGGESTER_ROLES = ["tag_exec"]` violates the rule (bare `"tag_exec"` literal) but the file is exempt because it also contains `hasAnyRole()` somewhere, rendering the check ineffective. Three production files (`app/api/flow/` routes) and two dashboard files (`location-selection.ts`, `widget-definitions.ts`) both import `hasAnyRole` or `ROLES.*` but still contain hardcoded role strings in their role lists and widget registries.

**The fix:** Replace file-level exemption with line-level exemption. A line calling `ROLES.ADMIN` is allowed; a bare literal string is not, regardless of what else the file imports.

### Earlier pattern-based attempts (part of root cause)

Two attempts using pattern matching both returned clean on a tree containing violations:

1. **First attempt:** `git grep -nE 'role\s*(===?|!=)\s*["\047]'` — Used POSIX ERE, which has no `\s`, so the pattern silently matched nothing. Empty result looked like a clean tree.

2. **Second attempt:** `grep -rnP 'role\s*(===?|!=)\s*["\047]' | grep -vE '//|/\*|\*'` — Pattern `(===?|!=)` cannot match `!==`. Comment filter was too broad, removing any line containing an asterisk. Method only looked for comparisons, missing roles in object values and array literals. Empty result looked like a clean tree.

**The lesson:** A pattern that returns empty on a tree with violations is indistinguishable from a clean tree unless the pattern is validated against a known hit first. Before reporting "clean", test that the check can find a planted violation.

## Solution

### Part 1: Full-tree audit (this story)

Parse the canonical role list from `lib/auth/role-labels.ts`:

```javascript
const roles = [...parseKeyedRoles('lib/auth/role-labels.ts')].map(([_, v]) => v);
// Result: ["tag_exec", "tag_csd", "tag_csm", "admin", "client_owner", ...]
```

Scan for quoted role literals in any context (comparison, object value, array membership), excluding:
- The four definition files (lib/auth/role-labels.ts, web/src/app/core/models/role.model.ts, scripts/create-user.mjs, scripts/admin.mjs)
- Files that import sanctioned helpers (hasRole, hasAnyRole, isRole, effectiveRole)
- Comments and route paths (quoted strings in `path: '...'`)

### Part 2: Periodic scanner (this story or follow-up)

Implement `scripts/audit-inline-roles.mjs`:
- Runs the full-tree scan
- Reports all violations and categorizes by severity
- Can be invoked on-demand or as a CI gate to catch drift

**Where it runs:** Add to the pre-push hook (`scripts/pre-push.mjs` or equivalent), so developers are warned before pushing. Alternatively, run as a `npm run audit:roles` command and add to CI. A script nobody invokes is the same failure mode as a check that only scans diffs.

## Acceptance criteria

**Scope changed after the audit landed.** This story was written as
document-and-propose, with AC7 explicitly stating "no fixes in this story".
Sam directed it be finished rather than handed on, so the fixes are here. The
original framing is recorded rather than overwritten, because the audit half
was completed under it.

1. [x] Full-tree audit completed and reported by scope, units stated separately:
   production 8 files / 26 lines / 52 occurrences; test 3 files / 10 lines / 10
   occurrences; legacy 135 lines across 29 files.
2. [x] Both holes documented: diff-scoping, and the file-level exemption.
3. [x] **All 8 production violations fixed.**
4. [x] A tree-wide scanner exists, `scripts/check-role-strings.mjs`, exempting
   per line rather than per file, parsing the role list from
   `lib/auth/role-labels.ts` rather than hardcoding it.
5. [x] The scanner runs in `pre-commit` and `pre-merge-commit`. It takes 0.06s,
   so it runs every time rather than being a periodic job somebody remembers.
6. [x] The scanner is verified in both directions: it catches a planted bare
   literal AND a planted `hasAnyRole(r, ["admin"])`, and does not flag
   `hasAnyRole(r, [ROLES.ADMIN])`.
7. [ ] CLAUDE.md still describes the constraint as enforced without saying that
   enforcement was, until now, diff-scoped only. **Left for Sam** — it is his
   instruction file.

## What was fixed

| File | Was | Now |
| --- | --- | --- |
| `lib/auth/admin.ts:175` | `role: "tag_exec"` in the claim-**issuing** path | `ROLES.TAG_EXEC`, with a comment saying why this location is the worst place for a literal |
| `app/api/meta/status/route.ts:24` | `currentRole !== "tag_exec"` | `hasRole(session.currentRole, ROLES.TAG_EXEC)` |
| `app/api/flow/card/[cardId]/suggestions/route.ts` | `SUGGESTER_ROLES = ["tag_exec", ...]` | `ROLES.*` |
| `app/api/flow/org/[orgId]/suggestions/route.ts` | `REVIEWER_ROLES = [...]` | `ROLES.*` |
| `app/api/flow/suggestions/[suggestionId]/resolve/route.ts` | `REVIEWER_ROLES = [...]` | `ROLES.*` |
| `lib/dashboard/location-selection.ts` | two role lists, **each written twice** | `AGENCY_DASHBOARD_ROLES` / `CLIENT_DASHBOARD_ROLES`, defined once |
| `lib/dashboard/widget-definitions.ts` | 28 literals across an `availableFor` map | `ROLES.*` throughout, imported from `role-labels` because this file is client-safe and `roles.ts` is `server-only` |
| `scripts/setup-csm-test-data.ts:110` | `role: "tag_csm"` | `ROLES.TAG_CSM` |

## Tasks

- [x] Implement a full-tree role scan finding all literals, not only comparisons
- [x] Document production violations by scope with file names and line numbers
- [x] Document the 135 legacy lines across 29 files with triage rationale
- [x] Analyse and document both holes: diff-scoping and the file-level exemption
- [x] Analyse why the first two pattern-based checks returned clean on a tree with violations
- [x] Build the scanner and wire it into `pre-commit` and `pre-merge-commit`
- [x] Close hole 2 by exempting per line rather than per file
- [x] Fix all 8 production violations
- [ ] CLAUDE.md wording — Sam's file, left to him

## Dev Agent Record

### The mistake worth recording

**The first version of this scanner reproduced hole 2 inside the fix for hole 2.**

It carried a line-level helper exemption: a line calling `ROLES.`, `hasRole(`,
`hasAnyRole(` and friends was skipped. That reasoning is superficially right and
actually wrong, and testing caught it. A planted probe with three lines:

```ts
export const a = (r: string) => r === "tag_exec";          // caught
export const b = (r: string) => hasAnyRole(r, ["admin"]);  // NOT caught
export const c = (r: string) => hasAnyRole(r, [ROLES.ADMIN]); // correctly ignored
```

Line b is hole 2 exactly — the call that makes it a violation is the call that
exempted it — and my line-level version let it through just as the file-level
version had.

The exemption was never needed. The pattern matches **quoted** literals, and
`ROLES.ADMIN` has no quotes, so sanctioned usage cannot false-positive. It was
removed entirely rather than narrowed. An exemption that cannot help and can
only hide is not a trade-off.

This is standing order 2, `AGENT_COORDINATION.md` §10: validate the instrument
before trusting a clean result. The scanner returned zero on the whole tree
before the probe was planted, and that zero would have been believed.

A second, smaller version of the same error: the first probe run appeared to
pass because `git ls-files` reads the index and the probe was untracked, and
the reported exit code was `grep`'s rather than the scanner's.

### Deliberately not done

- **`legacy/**` is excluded from the scanner**, not fixed. 135 lines across 29
  files, and story 10.7 deletes the directory. Some are genuine role-list
  definitions (`legacy/knowledge-base/roles.ts`, `legacy/nav.tsx` hat lists)
  rather than access checks, so a blanket conversion would be wrong.
- **Test files excluded.** A role literal in a routing assertion describes
  behaviour rather than gating it.
- **`functions/src/auth.ts` remains exempt.** It cannot import
  `lib/auth/roles.ts` across the workspace boundary, and it asserts every role
  it issues exists in `ROLES`. Story 14.A folds it into `app/api`, after which
  the exemption should be removed.

### Verification

```
node scripts/check-role-strings.mjs   EXIT 0 on the tree, 0.06s
                                      EXIT 1 on a planted bare literal
                                      EXIT 1 on a planted hasAnyRole(r, ["admin"])
                                      ignores hasAnyRole(r, [ROLES.ADMIN])
tsc --noEmit                          EXIT 0
npm run test                          635 tests, 55 files, all pass
```
