---
baseline_commit: db89f36
---

# Story 14.B — Inline role string audit and periodic scanner

**Epic:** 14 — Off Firestore  
**Status:** Draft  
**Depends on:** Completion of 10.7 (legacy removal)

## Story

As a security maintainer, I need to find all hardcoded role strings in the tree and implement a periodic audit to catch drift, so that the role-isolation constraint is enforced tree-wide rather than only on new commits.

## Problem

The pre-commit hook `scripts/check-story-status.mjs` includes an inline-role check that scans for hardcoded role strings. It is diff-scoped: it only examines files staged for commit.

**Design intent:** Guard against NEW violations entering the tree.  
**Design gap:** Cannot find EXISTING violations. The hook has successfully prevented new inline roles from entering the tree, but three live violations already exist.

CLAUDE.md describes the role constraint as "enforced via pre-commit hook" but is silent about whether that covers the whole tree or only new commits. The gap is documented in `AGENT_COORDINATION.md §3`: a check that reports healthy while scanning a shrinking share of what it claims to cover.

## Audit findings

Three live violations, verified against `lib/auth/role-labels.ts`:

| File | Line | Violation | Severity | Owner |
|---|---|---|---|---|
| `lib/auth/admin.ts` | 175 | `await setUserClaims(uid, [{ role: "tag_exec", locations: [] }]);` | **CRITICAL** | Story that wrote `promoteToExec` |
| `app/api/meta/status/route.ts` | 24 | `if (session.currentRole !== "tag_exec")` | High | Story that wrote Meta status route |
| `scripts/setup-csm-test-data.ts` | 110 | `role: "tag_csm"` | Low | Seed script, fixable now but low priority |

### Legacy violations (expected-to-vanish)

Ten more violations in `legacy/**` — all session role comparisons and array membership tests. Story 10.7 deletes these files; do not count as regressions when 10.7 lands.

### False positive (not a violation)

`web/src/app/app.routes.ts:102` — `path: 'admin'` is a route path, not a role string. Root cause: the check looks for quoted literals; route paths share text with role names by coincidence. This false positive is expected and documented.

## Why the checks failed

The audit was performed by deriving role values from `lib/auth/role-labels.ts` and searching for all quoted role literals in any context. Earlier attempts using pattern matching returned clean on a tree containing three violations:

1. **First attempt:** `git grep -nE 'role\s*(===?|!=)\s*["\047]'` — Used POSIX ERE, which has no `\s`, so the pattern silently matched nothing. Empty result looked like a clean tree.

2. **Second attempt:** `grep -rnP 'role\s*(===?|!=)\s*["\047]' | grep -vE '//|/\*|\*'` — Pattern `(===?|!=)` cannot match `!==` (the `=` characters don't line up with the quote position). Comment filter was too broad, removing any line containing an asterisk. Method only looked for comparisons, missing roles in object values and array literals. Empty result looked like a clean tree.

**The lesson:** A pattern that returns empty on a tree with violations is indistinguishable from a clean tree unless the pattern is validated against a known hit first. Before reporting "clean", test that the check can find a planted violation.

## Solution

### Part 1: Full-tree audit (this story)

Parse the canonical role list from `lib/auth/role-labels.ts`:

```javascript
const roles = [...parseKeyedRoles('lib/auth/role-labels.ts')].map(([_, v]) => v);
// Result: ["tag_exec", "tag_csd", "tag_csm", "admin", "client_owner", ...]
```

Scan for quoted role literals in any context (comparison, object value, array membership), excluding:
- The four definition files (lib/auth/role-labels.ts, web/src/app/core/models/role.model.ts, scripts/create-user.mjs, scripts/admin.mjs)
- Files that import sanctioned helpers (hasRole, hasAnyRole, isRole, effectiveRole)
- Comments and route paths (quoted strings in `path: '...'`)

### Part 2: Periodic scanner (this story or follow-up)

Implement `scripts/audit-inline-roles.mjs`:
- Runs the full-tree scan
- Reports all violations and categorizes by severity
- Can be invoked on-demand or as a CI gate to catch drift

**Where it runs:** Add to the pre-push hook (`scripts/pre-push.mjs` or equivalent), so developers are warned before pushing. Alternatively, run as a `npm run audit:roles` command and add to CI. A script nobody invokes is the same failure mode as a check that only scans diffs.

## Acceptance criteria

1. Full-tree audit completed using the method above
2. All three live violations identified and categorized by severity
3. Ten legacy violations documented as expected-to-vanish with Story 10.7 (no regressions when 10.7 lands)
4. False positive documented and explained
5. Root cause analysis: explain why the pre-commit hook is diff-scoped (correct for commit gate) and why CLAUDE.md is silent about tree-wide enforcement
6. Ownership assigned: each violation goes to the story that owns its file; no fixes in this story
7. Periodic scanner proposed with a specific location (pre-push hook, npm command, CI gate, or equivalent)

## Tasks

- [ ] Implement full-tree role scan using the method above
- [ ] Document the three live violations and their owners
- [ ] Document the ten legacy violations as expected-to-vanish
- [ ] Document the false positive and explain why it is not a violation
- [ ] Analyze why the first two pattern-based checks returned clean on a tree with violations
- [ ] Propose the periodic scanner and where it runs
- [ ] Verify the audit method catches all three violations and the false positive (validation step)

## Dev notes

**On validation:** The audit method relies on parsing the real role list rather than hardcoding a pattern. This is more maintainable (changes to the role list propagate automatically) and less error-prone than regex. Before finalizing the scanner, test that it catches all three known violations and correctly rejects the false positive.

**On ownership:** `lib/auth/admin.ts:175` is the critical fix — a hardcoded role string in the claim-issuing path. Whoever owns the story that wrote `promoteToExec` should handle this. The other two are lower priority and can be fixed when their owning stories are touched.

**On CLAUDE.md clarity:** The contract is enforced for new commits (via diff-scoped hook). The tree-wide enforcement is missing. Update CLAUDE.md or add a separate story to clarify what is covered by the hook vs. what requires periodic audit.
