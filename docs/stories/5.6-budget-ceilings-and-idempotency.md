# Story 5.6 — Budget ceilings and idempotency

**Epic:** 5 — Onboarding and campaign launch
**Status:** Implemented, unit-tested — landed alongside Story 5.4. AC5's double-submit test is written and passing ([test/campaign-launch.test.ts](../../test/campaign-launch.test.ts)), but against a mocked Meta client and an in-memory fake Firestore, not a real or sandboxed Meta API call — this dev environment has no `META_SYSTEM_USER_TOKEN`/`META_BUSINESS_ID`/etc. configured. Not marking Done until the same double-submit scenario is verified against a real Meta ad account (Phase 2 of `docs/meta-live-launch-plan.md` produced a live token, but it isn't available in this environment).

## Story

As TAG, I want to ensure campaigns never exceed budget ceilings and can safely be re-created if something fails, so that we don't accidentally overspend or leave a client half-configured.

## Context

**Budget ceilings:** each offer has a max monthly spend (e.g., "tax prep max $2000/mo"). Form (5.2) enforces this; this story enforces it server-side.

**Idempotency:** if campaign creation (5.4) fails mid-way (e.g., campaign created, ad set created, ads fail), a retry should not duplicate. Store the created campaign ID so a second call skips creation and returns the same ID.

## Acceptance criteria

1. Each offer has a configured max budget (hardcoded for MVP).
2. On campaign creation (5.4): validate requested budget ≤ max. Reject if over.
3. Store campaign ID in Firestore at creation time (Story 3.5 audit log, or separate).
4. On retry of creation: check Firestore for existing campaign ID, return it instead of re-creating.
5. Test: creating twice with same inputs returns same campaign ID both times.

## Tasks

- [x] Define max budget per offer (hardcoded) — [lib/onboarding/offer-budgets.ts](../../lib/onboarding/offer-budgets.ts)
- [x] Server-side budget validation in Story 5.4 — checked before any Meta call in `createPausedCampaign`
- [x] Create campaign state store — [lib/onboarding/campaign-launch-store.ts](../../lib/onboarding/campaign-launch-store.ts), at `locations/{locationId}/campaignLaunches/{key}` rather than `campaigns/{locationId}/{campaignId}` (keyed by the idempotency key so it can be found *before* a campaign ID exists, which a fresh request needs)
- [x] Idempotency check: on 5.4 retry, look up by (locationId, templateId, formInputs hash)
- [x] Test: creating twice with identical inputs returns the same campaignId both times, and only one `POST /campaigns` call is made — [test/campaign-launch.test.ts](../../test/campaign-launch.test.ts) (mocked Meta client + fake Firestore)
- [ ] Verify the same double-submit scenario against a real or sandboxed Meta ad account before marking this story Done — needs `META_SYSTEM_USER_TOKEN` etc. configured, not available in this environment

## Dev notes

Idempotency key: hash of (locationId, templateId, form inputs) so accidental re-submits (double-click, retry) don't duplicate. Implemented with `campaignLaunchKey()`.

State store: tracks created campaignId/adSetId/adIds incrementally (written after each Meta call succeeds, not just at the end) specifically so a retry after a mid-way failure resumes from the last completed resource instead of restarting. Final document has `status: "paused"`; a retry against a `"paused"` document short-circuits and returns the stored result without any further Meta calls.
