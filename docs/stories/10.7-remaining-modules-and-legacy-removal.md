# Story 10.7 — Remaining feature modules and legacy removal

**Epic:** 10 — Angular migration
**Status:** Draft

## Story

As the team maintaining this codebase, I want the last Next screens ported and
then deleted, so that every feature exists in exactly one place and the Next
deployment is an API host serving a bundle, nothing more.

## Context

What is left after 10.5 and 10.6: onboarding (checklist and the launch wizard),
FLOW, courses, admin (tenants, users, courses), success, bug reports, the setter
dashboard, and the Meta, Drive and Slack service modules that have no screens of
their own but back widgets and tabs elsewhere.

This story is where the migration either finishes or acquires a permanent tail.
The failure mode is leaving three Next screens alive because they are small,
which keeps the whole Next rendering path, its layout, its auth wiring and its
`lib/` imports in the tree forever. The deletion is the deliverable, not the
cleanup after it.

**One open question this story must answer first.** The setter dashboard has no
home in `docs/frontend-file-tree.md`. It reads GHL speed-to-lead data, so it is
plausibly `features/ghl/`, but it is a distinct role's whole workspace rather
than a GHL screen. Decide and record it in the file tree before building, since
the tree and the lint zones are written from each other.

## Acceptance criteria

1. `features/onboarding/`, `features/flow/`, `features/courses/`,
   `features/admin/`, `features/success/`, `features/bug-reports/` exist, lazy
   and `OnPush`.
2. `features/meta/`, `features/drive/` and `features/slack/` hold their typed
   services and any widgets, and import no sibling integration.
3. The setter dashboard has a recorded home in the file tree and lives there.
4. Every remaining server action has an endpoint equivalent, and
   `grep -rl '"use server"' app` returns nothing.
5. No file under `app/` renders a page. `app/api/**` and the bundle host remain.
6. `docs/frontend-file-tree.md` and `web/eslint.config.js` agree, in the same
   commit, as they must.
7. `ng build --configuration production && ng lint && ng test --watch=false`
   pass, and the root gate passes for the API-only Next app.

## Endpoint inventory (Story 11.5)

Measured at `40e9aa6` by `node scripts/inventory-endpoints.mjs --area=<area>`.
Re-run before starting; the surface moves.

| Area | Server Actions | Existing routes | Pages importing `lib/` |
|---|---|---|---|
| `admin` | 20 across 4 files | 0 | 7 of 7 |
| `onboarding` | 3 | 0 | 4 of 4 |
| `courses` | 0 | 0 | 2 of 2 |
| `knowledge-base` | 0 | 0 | 2 of 2 |
| `setter` | 1 | 0 | 1 of 1 |
| `success` | 0 | 0 | 1 of 1 |
| `signin` | 0 | 0 | 1 of 1 |
| `root` | 1 | 0 | 1 of 1 |
| **Total** | **25 across ~10 files** | **0** | **19 of 19** |

**`admin` is the surprise here and it changes this story's shape.** Twenty
actions across four files is nearly half of the app's entire remaining Server
Action surface, more than 10.5 and 10.6 combined. This story was scoped as "the
small leftovers"; the admin module alone is not small.

Worth deciding before starting whether `admin` should be split into its own
story rather than absorbed here. The rest of this story genuinely is small: five
areas with a combined 5 actions and 11 pages.

`signin` should already be gone by the time this story runs — Story 10.2's
remaining cutover task deletes it.

## Tasks

- [ ] Decide and record where the setter dashboard belongs
- [ ] Inventory the remaining endpoints, per Story 11.5
- [ ] `features/onboarding/` — checklist at `/onboarding/:locationId`, and the
      launch wizard as one component plus a form-state service
- [ ] `features/flow/` — framework view, suggestions, review queue
- [ ] `features/courses/` — player and progress
- [ ] `features/admin/` — tenants, users, courses
- [ ] `features/success/`, `features/bug-reports/`, setter
- [ ] `features/meta/`, `features/drive/`, `features/slack/` services
- [ ] Delete every remaining Next page, layout and server action
- [ ] Confirm `lib/` is reachable only from `app/api/**` and `functions/`
- [ ] Carried forward from the resweep, and not to be lost in the port:
  - [ ] Campaign activation is a separate, explicit step from creation, with
        copy saying it starts real spend. Story 5.5's action existed with zero
        call sites and every campaign stayed paused in Meta; do not fold
        activation back into the create button
  - [ ] The Fulfillment stage matcher parses GHL's real stage names
        ("AP 2 - Ads Launched"), not bare codes. `parseFulfillmentStage` is the
        single implementation
  - [ ] A failed course-progress save restores the previous checkbox value
        rather than deleting the entry, and says so. Deleting reads as
        unchecked and erases real completion from the screen
  - [ ] The setter dashboard's live refresh is visible when it fails. Its Next
        version polled a route that did not exist and sat frozen looking live,
        on a board whose entire purpose is a two-minute window
  - [ ] Cost-per-conversion is labelled as such, not as ROAS. The Meta insights
        call carries no revenue, so a real ROAS cannot be derived from it, and
        the direction reverses with the name

## Dev notes, deletion is the deliverable

Epic 10's rule is that when an Angular feature passes its gate, the matching
Next pages are deleted in the same commit. This story is where that rule pays
off or is quietly abandoned.

A useful check at the end: the count of `page.tsx` files under `app/` should be
zero, and `grep -rl '"use server"' app` should return nothing. Both are cheap to
run and neither can be satisfied by a screen that merely looks ported.

## Dev notes, what stays

`app/api/**`, `lib/`, `functions/`, and the Next app as a same-origin host for
the Angular bundle. `hub_session` is httpOnly and `SameSite=lax` and only
survives that topology, so the host is not incidental.
