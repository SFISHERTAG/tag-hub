# Story 5.1 — Onboarding checklist from Fulfillment stages

**Epic:** 5 — Onboarding and campaign launch
**Status:** Done

## Story

As a CSM, I want an onboarding checklist that tracks where each new client sits in setup, so that nothing falls through and handoffs are clear.

## Context

Each client has a Fulfillment opportunity tracking their onboarding stage:
- PR1: Funnel built
- PR2: Funnel tested
- AP1: Campaign created (paused)
- AP2: Campaign launched
- AP3–5: Post-launch (up-sell, ascension)

The checklist maps to this pipeline: each stage has a set of tasks, and advancing the stage closes those tasks.

## Acceptance criteria

1. `/onboarding` shows checklist driven by the client's Fulfillment opportunity stage.
2. Stage map: each stage (PR1–AP5) has a list of "before next stage" tasks (e.g., "Fund the account" for PR1). **Before AP1 specifically:** "Connect Meta ad account" — Story 5.4 (create paused campaign) calls the Marketing API against a specific `metaAdAccountId` on the tenant record, so AP1 cannot complete without it. Two-step process, documented in `docs/meta-live-launch-plan.md`: (a) client's business partners their ad account with TAG's Business Manager (Business Settings → Partners, on the client's side), (b) TAG assigns that ad account to the `tag-hub-server` System User (Business Settings → System Users → Add Assets → Ad Accounts, on TAG's side) — partner access alone isn't enough, the System User needs the individual asset assignment too, confirmed the hard way during Story 4.1. Once both are done, the id gets set on the tenant either via intake (if the client already had an account — see `functions/src/webhooks/phase2-intake-submit.ts`) or manually via the admin tenant form (`app/admin/tenants/[locationId]/tenant-form.tsx`, which already has the field).
3. CSM can mark tasks complete via checkbox (optional — not gated by Fulfillment stage).
4. Advancing stage (via Story 5.5) auto-completes tasks for that stage.
5. Checklist is read-only for client users (only CSM can edit).
6. Each client shows **time in current stage**, derived from the Fulfillment
   opportunity's `lastStageChangeAt` — the same field `daysSince()` already
   reads on the pipeline board. Without it the checklist says what is
   outstanding but not what is *stuck*, and a client parked in PR2 for three
   weeks looks identical to one that arrived yesterday.

## Tasks

- [x] Define stage → task mapping (PR1: [task1, task2, ...], etc.) — `lib/onboarding/stage-tasks.ts`
- [x] Query Fulfillment opportunity to get current stage — `getFulfillmentOpportunity()` in `lib/ghl/portfolio.ts`
- [x] Component: OnboardingChecklist showing current stage + tasks — `app/onboarding/checklist.tsx`
- [x] Server action: markTaskComplete() (CSM only) — `app/onboarding/actions.ts`
- [x] Stage timer from `lastStageChangeAt`, surfaced per card — reuses `daysSince()` from `lib/ghl/opportunities.ts`
- [x] Route: `/onboarding` or section of `/portfolio` when CSM enters client — `app/onboarding/page.tsx`, resolved from the impersonation cookie set by entering a client via Portfolio (3.3), same pattern as 5.4's launch flow

## Dev notes

Tasks are fixed per stage for MVP. Customization per client is a fast-follow.

Story 5.5 (dedicated Fulfillment stage-advancement UI) isn't built yet. The
Fulfillment pipeline already renders on the generic pipeline board
(`app/l/[locationId]/pipeline`) alongside Sales, so `StageControl` /
`moveOpportunityStagAction` is already the stage-advance mechanism for it.
AC4's auto-complete hook lives there: `StageControl` resolves the departed
stage's name and passes it to `moveOpportunityStagAction`
(`app/l/[locationId]/pipeline/actions.ts`), which auto-completes that
stage's tasks via `completeStageTasks()` when the name matches a known
Fulfillment stage. When 5.5 ships its own control, wire it through the same
action rather than duplicating this.

`getFulfillmentStage()` (old, in `lib/ghl/portfolio.ts`) returned the wrong
field (opportunity name, not stage) and had no way to resolve a stage id to
a name. Replaced with `getFulfillmentOpportunity()`, which finds the
Fulfillment pipeline by name (`pipeline.name === "Fulfillment"`, no tenant
field carries a pipeline id) and resolves `pipelineStageId` to a stage name
via that pipeline's own stages.

## Superseded stage model (2026-08-22)

Status unchanged, and the code this story landed still works. The note is here
because `docs/fulfillment-pipeline-architecture.md` re-draws what the stages
mean, and anyone reading this story afterwards would otherwise implement against
a model TAG no longer runs.

What changes for this story's code:

- `STAGE_TASKS` needs remapping. The new PR1–AP5 are ownership handoffs (TAG,
  client, TAG, client, TAG, client), not the build sequence assumed here.
- AC2's two-step Meta handoff becomes a **track**, not an AP1 task. It runs
  parallel to build and gates launch, which a task inside one stage cannot
  express.
- AC6's `lastStageChangeAt` becomes a fallback. Stage entry time comes from the
  dated milestone custom fields instead, because `lastStageChangeAt` only
  describes the current stage and loses the duration of every stage already left.

The remapping itself is not scheduled. It belongs with story 5.7, which is the
first consumer that depends on the new meanings.
