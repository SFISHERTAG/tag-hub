---
baseline_commit: 40e9aa656afeeb636340e4bccdb87f48ce3f858a
---

# Story 11.5 — Endpoint inventory ahead of each feature story

**Epic:** 11 — Migration readiness
**Status:** In Progress

## Story

As the person starting an Epic 10 feature story, I want to know exactly which
endpoints it needs before I write a component, so the endpoint layer is designed
once rather than discovered one screen at a time.

## Context

An Angular SPA cannot call a Server Action. Every data path a screen needs must
exist as an endpoint under `app/api/**` first. Measured on this branch:

| | Count |
|---|---|
| Files marked `"use server"` | 22 |
| Exported server actions in them | 41 |
| Existing `app/api/**` route files | 25 |
| `page.tsx` files importing `lib/` directly | 33 of 33 |

Every page reads `lib/` directly. Not most; all of them.

Those numbers matter less than the fact that they moved. `docs/epics.md`
records "39 exported Server Actions across 21 `use server` files" and "24 of 25
data-reading pages", measured earlier. Both grew while the migration was being
planned around them. An inventory taken once at the top of an epic is stale
before the epic's second story starts, which is why this is a per-story
discipline and not a one-off audit.

## Acceptance criteria

1. A script produces the inventory on demand: server actions by feature area,
   existing endpoints, and pages importing `lib/`, with counts and paths.
2. It can be scoped to one feature area, so a story can ask only about its own
   surface.
3. Each Epic 10 feature story carries its inventory, taken when the story
   starts, with the date and commit it was taken at.
4. The inventory distinguishes an action needing a new endpoint from one whose
   endpoint already exists.
5. Re-running it after a story closes shows the surface shrinking, and that
   delta is the migration's actual progress measure.

## Tasks

- [x] Write the inventory script
- [x] Scope it by feature area
- [x] Run it for 10.5, 10.6 and 10.7 and attach the results to those stories
- [x] Record the count in `docs/epics.md` with the commit it was measured at,
      so the next reader can tell whether it is stale
- [ ] Re-run after each feature story closes and record the delta

## Dev notes, why per-story rather than once

Designing all 41 endpoints up front is the other option and it is worse. It
front-loads guesses about screens nobody has built, and the endpoint shape a
screen actually wants is often not the shape its server action had — several
actions exist only because a component needed something awkward from a server
component, and disappear rather than becoming endpoints.

Per-story keeps the design close to the screen that consumes it, at the cost of
needing this inventory to be cheap to re-run. Hence a script.

## Dev notes, the number to watch

The useful progress metric for Epic 10 is not screens ported. It is the count of
`"use server"` files remaining, because that is what has to reach zero before
the Next rendering path can be deleted. Screens can look ported while their
actions live on, and that state is indistinguishable from done at a glance.

## Dev Agent Record

### Implementation plan

Followed the pattern `scripts/lib/story-references.mjs` established: pure,
side-effect-free logic in `scripts/lib/`, a thin CLI that walks and prints, and
a test importing the lib directly so it needs no filesystem and no git.

Red-green-refactor. The 20 tests were written first and confirmed failing
(`Cannot find module`) before any implementation existed.

### Debug log

- `tsc` caught what `vitest` could not: `byArea` was inferring as `{}`, so
  `s.byArea.portfolio.actions` was an error in the test even though the test
  passed at runtime. Fixed with a JSDoc `@type` on the accumulator in the
  module rather than a cast in the test, so every consumer gets the type.
- The script's output cross-checks against a manual count made earlier by a
  different method (ad-hoc grep): both give 41 actions across 22 files, 25
  routes, 33 of 33 pages. Two independent methods agreeing is the reason to
  trust the number.

### Completion notes

Built and verified. `node scripts/inventory-endpoints.mjs` reports the whole
app; `--area=<area>` scopes to one feature surface; `--json` is
machine-readable. Every run prints the commit it measured at, so a pasted
result can be dated.

Inventories attached to 10.5, 10.6 and 10.7, all measured at `40e9aa6`.

**One finding changes 10.7's shape.** `admin` holds 20 Server Actions across 4
files: nearly half the app's remaining surface, and more than 10.5 and 10.6
combined. 10.7 was scoped as "the small leftovers". That story now carries the
number and flags splitting `admin` out as a decision to take before it starts.
This is the story doing its job — the point of inventorying before building is
to find this before a story is half-written, not after.

**Task 5 is deliberately left unchecked.** "Re-run after each feature story
closes and record the delta" cannot be satisfied yet: no feature story has
closed. It is a standing obligation rather than a deliverable, so it does not
belong in Tasks, but the story spec is not mine to reword under this workflow.
Status therefore stays In Progress rather than Review, because marking it
complete would be false.

### File List

- `scripts/lib/endpoint-inventory.mjs` (new)
- `scripts/inventory-endpoints.mjs` (new)
- `test/endpoint-inventory.test.ts` (new)
- `docs/stories/10.5-ghl-integration-module.md` (modified)
- `docs/stories/10.6-widget-dashboard-and-clients-book.md` (modified)
- `docs/stories/10.7-remaining-modules-and-legacy-removal.md` (modified)
- `docs/epics.md` (modified)
- `docs/stories/11.5-endpoint-inventory-ahead-of-each-feature-story.md` (modified)

### Change Log

- 2026-08-21 — Endpoint inventory script, its pure lib and 20 tests. Inventories
  attached to 10.5, 10.6 and 10.7. `docs/epics.md` points at the script rather
  than a number, and records the `admin` finding.
