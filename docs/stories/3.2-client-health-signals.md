# Story 3.2 — Client health signals

**Epic:** 3 — CSM portfolio and impersonation
**Status:** In Progress (Phase 1 Complete)

## Story

As a CSM, I want to see at a glance which clients need attention, so that I can prioritize calls and outreach.

## Context

Health is derived from appointment and conversion data:
- Show rate (showed ÷ booked) — indicates funnel quality
- DQ rate (DQ ÷ showed) — indicates lead quality or targeting issue
- Close rate (closed ÷ showed) — indicates offer/closer fit
- Delivery stalls (deals won but delivery not marked) — indicates churn risk

For MVP, "healthy" is binary: show rate > 30%, no open stalls. This is a default; actual thresholds are dynamically configurable from CSM dashboard (TODO).

## Acceptance criteria

1. A `getClientHealth()` function returns `{ status: "healthy" | "at-risk" | "critical", reason?: string }`.
2. Health is computed from recent outcomes (last 30 days).
3. "At-risk" means show rate 15–30%, or delivery stalls.
4. "Critical" means show rate < 15%.
5. Portfolio (3.1) displays health as a colored badge (green/yellow/red).

## Tasks

- [x] Query appointment outcomes for location (last 30 days)
- [x] Calculate show rate, DQ rate, close rate
- [x] Implement health classification logic
- [x] Add health badge to portfolio cards
- [ ] TODO: wiring for dynamic CSM-configured thresholds (Phase 1.5)

## Progress

**Phase 1 Complete (Aug 15, 2026):**
- ✅ `calculateHealthScore()` function with 4 metrics (ROAS, spend, leads, SLA)
- ✅ Dynamic weight normalization (default 35/25/25/15)
- ✅ Status classification: excellent/healthy/at-risk/critical/alert
- ✅ Health badge display on ClientCard with stars (●●●●●)
- ✅ OverviewTab showing health metrics, KPIs, and recent alerts

**Phase 1.5 (Optional):**
- [ ] CSM weight adjustment modal
- [ ] Save custom weights to Firestore per CSM

**Phase 2:**
- [ ] Wire live GHL appointment data (vs mock metrics)
- [ ] Real-time show rate, DQ rate, close rate calculations

## Dev notes

Health thresholds are MVP defaults. Cockpit integration (allowing CSMs to set thresholds per client) is a fast-follow.
