# Story 14.1 Audit — Firestore call sites and data paths

**Date:** 2026-08-23 (rewritten from code)
**Baseline:** `635e14e` (`main`)
**Scope:** app-side Firestore access — `lib/**` and `app/api/**`. `functions/**` is
out of scope; see "Why functions/ is excluded" below.

> This file replaces an earlier version audited at `3b3f65c`. That version was
> derived from filenames rather than from code and was wrong in the file list,
> in nearly every collection name, and in its transaction finding. The
> corrections are itemised at the end under "What the previous audit got wrong",
> because later stories were sequenced off those numbers.

---

## Summary

- **20 files** call `firestore()`. 18 in `lib/**`, 2 in `app/api/**`.
- **1 implementation file**, `lib/firestore.ts` (30 lines), which is the only
  place the SDK is constructed.
- **21 distinct document paths**, of which **10 are parent-scoped
  subcollections**. There is no flat 14-collection model.
- **5 operations beyond get/set/query**: 2 transactions, 1 batch, 1 collection
  group query, 1 multi-document `getAll`.
- **2 test files** (`lib/ghl/store.test.ts`, `lib/meta/retry.test.ts`) reach the
  SDK and will need the seam's fake.

---

## Call sites

### `lib/**` (18)

| File | Paths touched | Operations |
| --- | --- | --- |
| `lib/audit/store.ts` | `locations/{locationId}/auditLog` | add, query, get, **runTransaction** (:148) |
| `lib/auth/groups.ts` | `groups` | get, list, query, add, update, delete |
| `lib/auth/otp.ts` | `authCodes/{hash}`, `authCodeCooldowns/{hash}` | get, set, delete, **runTransaction** (:140) |
| `lib/bug-reports.ts` | `bugReports` | add, query |
| `lib/dashboard/csm-clients.ts` | `clients`, `clients/{clientId}/alerts` | query, get, merge-set |
| `lib/dashboard/csm-directory.ts` | `csm/{email}` | get, list, query, set |
| `lib/dashboard/freshness.ts` | `locations/{locationId}/appointmentOutcomes` | query |
| `lib/dashboard/location-config.ts` | `locations/{locationId}` | get |
| `lib/ghl/store.ts` | `ghl/agency`, `ghl/agency/companies/{companyId}`, `ghl/agency/locations/{locationId}`, `locations/{locationId}/appointmentOutcomes/{appointmentId}`, `locations/{locationId}/settings/followUp` | get, set, list, **getAll** (:256) |
| `lib/ghl/tenants.ts` | `locations/{locationId}` | get, merge-set, **select() projection** (:97) |
| `lib/knowledge-base/db.ts` | `manual_pages/{pageId}`, `manual_pages/{pageId}/versions/{versionId}` | get, ordered query, **batch** (:91) |
| `lib/meta/conversions.ts` | `locations/{locationId}/metaConversionLog/{eventType}_{entityId}` | set |
| `lib/meta/fetch-log.ts` | `locations/{locationId}/metaFetchLog/latest` | get, set |
| `lib/meta/retry.ts` | `metaConversionLog` | **collectionGroup query** (:155) |
| `lib/onboarding/campaign-launch-store.ts` | `locations/{locationId}/campaignLaunches/{key}` | get, set, update |
| `lib/onboarding/store.ts` | `locations/{locationId}/onboardingChecklists/{opportunityId}` | get, set |
| `lib/webhooks/deadLetterQueue.ts` | `webhookDeadLetter` | add, get, update, query |
| `lib/webhooks/idempotency.ts` | `webhookEventsProcessed/{source}:{eventId}` | get, set, delete — **no importers, see below** |

### `app/api/**` (2)

| File | Paths touched | Operations |
| --- | --- | --- |
| `app/api/clients/[clientId]/creatives/route.ts` | `clients/{clientId}/meta_creatives` | query |
| `app/api/clients/_lib/client-record.ts` | `clients/{clientId}` | get |

`app/api/admin/tenants/[locationId]/route.ts` and `app/api/portfolio/tenants/route.ts`
mention `firestore()` in comments only. They are not call sites.

---

## Path inventory

Ten of these are subcollections under a parent key. A flat
`Repository.auditLog.list()` cannot express them, which is why 14.1 uses
path-scoped accessors.

### Top-level (11)

| Path | Owner |
| --- | --- |
| `authCodes/{sha256(email)}` | `lib/auth/otp.ts` |
| `authCodeCooldowns/{sha256(email)}` | `lib/auth/otp.ts` |
| `groups/{id}` | `lib/auth/groups.ts` |
| `csm/{email}` | `lib/dashboard/csm-directory.ts` |
| `clients/{clientId}` | `csm-clients.ts`, `app/api/clients/_lib/client-record.ts` |
| `bugReports/{id}` | `lib/bug-reports.ts` |
| `manual_pages/{pageId}` | `lib/knowledge-base/db.ts` |
| `webhookDeadLetter/{id}` | `lib/webhooks/deadLetterQueue.ts` |
| `webhookEventsProcessed/{source}:{eventId}` | `lib/webhooks/idempotency.ts` |
| `locations/{locationId}` | `ghl/tenants.ts`, `dashboard/location-config.ts` |
| `ghl/agency` | `lib/ghl/store.ts` |

### Parent-scoped (10)

| Path | Parent | Owner |
| --- | --- | --- |
| `ghl/agency/companies/{companyId}` | agency root | `lib/ghl/store.ts` |
| `ghl/agency/locations/{locationId}` | agency root | `lib/ghl/store.ts` |
| `clients/{clientId}/meta_creatives` | client | `app/api/clients/[clientId]/creatives/route.ts` |
| `clients/{clientId}/alerts` | client | `lib/dashboard/csm-clients.ts` |
| `locations/{locationId}/auditLog` | location | `lib/audit/store.ts` |
| `locations/{locationId}/appointmentOutcomes` | location | `ghl/store.ts`, `dashboard/freshness.ts` |
| `locations/{locationId}/settings/followUp` | location | `lib/ghl/store.ts` |
| `locations/{locationId}/metaConversionLog/{eventType}_{entityId}` | location | `meta/conversions.ts`, `meta/retry.ts` |
| `locations/{locationId}/metaFetchLog/latest` | location | `lib/meta/fetch-log.ts` |
| `locations/{locationId}/onboardingChecklists/{opportunityId}` | location | `lib/onboarding/store.ts` |
| `locations/{locationId}/campaignLaunches/{key}` | location | `lib/onboarding/campaign-launch-store.ts` |
| `manual_pages/{pageId}/versions/{versionId}` | manual page | `lib/knowledge-base/db.ts` |

---

## Operations the interface must carry

| Operation | Site | Why it matters for 14.2+ |
| --- | --- | --- |
| `runTransaction` | `lib/auth/otp.ts:140` | Verify-and-consume of an OTP. Read-modify-write under contention; the whole point is that two verifies cannot both win. |
| `runTransaction` | `lib/audit/store.ts:148` | Amend an audit entry. Must stay atomic against the immutability guarantee. |
| `batch` | `lib/knowledge-base/db.ts:91` | Write the version snapshot and the page update together. A partial write loses revert history. |
| `collectionGroup` | `lib/meta/retry.ts:155` | Scans `metaConversionLog` across every location for failed conversions. No per-parent equivalent — the retry job does not know which locations have failures. In Postgres this is the unqualified table scan, which is easier, but the seam must expose it or the retry job cannot be written. |
| `getAll` | `lib/ghl/store.ts:256` | Multi-document read by ref list. Degrades to N round trips if the seam only offers `get(id)`. |
| `select()` | `lib/ghl/tenants.ts:97` | Projection to `locationId` only, over every location. Dropping it turns a key scan into a full document read. |

`FieldValue.increment` is **not used anywhere in this repo.** The only occurrence
is a comment at `lib/auth/otp.ts:131` explaining why it was *not* used.

---

## Two leaks to close

**`lib/ghl/store.ts:33` re-exports the handle.** `export { firestore };` means
any module importing `lib/ghl/store` can obtain a raw Firestore instance without
importing `lib/firestore` at all. A lint rule keyed on the import path will not
catch this. The re-export has to go, and the rule needs a second check for the
symbol.

**`lib/webhooks/idempotency.ts` has no importers.** It is a hand-synced mirror of
`functions/src/lib/webhooks/idempotency.ts`, which is the live one. The two files
disagree about which is authoritative: the `lib/` header says the `functions/`
copy is the duplicate, the `functions/` header says `lib/` is the source of
truth. Nothing in `lib/` or `app/` imports the `lib/` copy. It is dead code kept
alive by a comment, and it should be deleted when the `functions/` fold lands
rather than seamed here.

---

## Why `functions/` is excluded

`functions/` is a separate npm workspace with `rootDir: ./src` and no path
mapping to `lib/`. Nothing in `functions/src` imports `lib/` and it cannot,
so a `lib/data/` module is unreachable from there. It also pins
`@google-cloud/firestore` `^7.11.0` against root's `^8.7.1` — different majors,
so a shared module would need those reconciled first.

Decided 2026-08-23: `functions/` folds into `app/api` in its own story. Its three
webhook endpoints become Next route handlers, which collapses the duplicated
Firestore init, idempotency, GHL client, Slack, Drive and Postgres modules at the
same time. Seaming `functions/src/firestore.ts` here would be work thrown away by
that fold.

---

## `docs/data-model.md` disagrees with the code

Flagged, not fixed here. `docs/data-model.md` is the contract under CLAUDE.md, so
correcting it is its own change.

| `data-model.md` says | Code has |
| --- | --- |
| `bug_reports` | `bugReports` |
| `orgs` | no call site |
| `users` | no call site — `lib/auth/groups.ts` uses `groups` |
| `flow_scripts` | no Firestore call site (Postgres only) |
| `creatives` | no top-level call site — `clients/{id}/meta_creatives` |
| not listed | `csm`, `webhookDeadLetter`, `webhookEventsProcessed`, `clients`, `clients/{id}/alerts`, `locations/{id}/appointmentOutcomes`, `locations/{id}/settings/followUp`, `locations/{id}/metaConversionLog`, `locations/{id}/metaFetchLog`, `locations/{id}/onboardingChecklists`, `locations/{id}/campaignLaunches` |

`data-model.md:264` also claims "29 modules call `firestore()`". The count is 20.

---

## What the previous audit got wrong

Recorded because 14.3–14.10 were sequenced off it.

1. **Included two files that do not touch this seam.**
   `functions/src/webhooks/phase1-provisioning.ts` and
   `functions/src/webhooks/phase3-meta-setup.ts` import `../firestore.js`, the
   `functions/` module, not `@/lib/firestore`.
2. **Missed four real call sites.** Both `app/api/**` files,
   `functions/src/webhooks/phase2-intake-submit.ts`, and the `lib/` copy of
   `idempotency.ts`.
3. **Named collections that do not exist:** `tenants`, `ghl_subscriptions`,
   `meta_campaigns`, `deadLetterQueue`, top-level `auditLog`, top-level
   `creatives`, `orgs`, `users`. `lib/ghl/tenants.ts:24` sets
   `TENANTS_COLLECTION = "locations"` — the module is named for a concept that
   has no collection.
4. **Reported "Transaction usage: None found."** There are two `runTransaction`
   sites and one `batch`, which is the same count `docs/epics.md` already
   documented as verified in code.
5. **Attributed `FieldValue.increment` to `lib/bug-reports.ts` and
   `lib/onboarding/store.ts`.** Neither uses it. Nothing does.
6. **Missed the collection group query, the `getAll`, the `select()`
   projection, and the `lib/ghl/store.ts` re-export.**
7. **Modelled the data as 14 flat collections.** Half the real paths are
   parent-scoped.
